#include<stdio.h>
struct Student()
{
	int b;
	char rollno[100],name[100];
	float per;
};
int main()
{
	struct Student s;
	gets(b);
scanf("%[^\n]",rollno);s
scanf("%[^\n]",name);
scanf("%f",per);
}


